# Tasks: FR-004 Docs Explorer Actions Menu (v1)

- [x] T1 — Add `contributes.submenus` entry `specmesh.docsExplorerActions` ("More Actions", `$(ellipsis)` icon) in [package.json](../../package.json)
- [x] T2 — Add `view/title` menu entry wiring the submenu to `specmesh.docsExplorer` in the `navigation` group in [package.json](../../package.json)
- [x] T3 — Add `menus["specmesh.docsExplorerActions"]` listing `specmesh.showOrphans`, `specmesh.showMissing`, `specmesh.refresh` in order in [package.json](../../package.json)
- [x] T4 — Compile and check for errors
